"""Integration tests for FDB Record Layer.

These tests require a running FoundationDB instance.
Set FDB_CLUSTER_FILE environment variable to point to your cluster file.

Run with:
    pytest tests/integration/ -v
"""
